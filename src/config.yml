---
# control
control:
  data_name: MNIST
  model_name: linear
  data_mode:
    num_splits: 2
    split_mode: horiz
    stat_mode: iid
  dist_mode:
    agg_mode: sync
    active_ratio: 1.0
    batch_size: 250
    num_steps: 100
    eval_mode: server
# experiment
pin_memory: True
num_workers: 0
init_seed: 0
num_experiments: 1
log_interval: 0.25
device: cpu
resume_mode: 0
profile: False